#!/bin/bash

echo "Running pre-checkout hook from my-plugin..."
printf "%s\n" "$(buildkite-agent secret get BITBUCKET_SSH_UCLOUD)" > ~/.ssh/id_rsa
chmod 600 ~/.ssh/id_rsa
ls -la ~/.ssh
echo $(realpath ~/.ssh) 
